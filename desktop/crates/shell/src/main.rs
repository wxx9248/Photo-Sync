//! The desktop application.
//!
//! Scope today is a placeholder entry point. The server, the storage adapters it drives, and
//! the user interface arrive in milestones M1 and M7, described in `docs/ROADMAP.md`.

fn main() {
    println!("photo-sync {}", env!("CARGO_PKG_VERSION"));
}
